[project]
name = "approval-workflow-service"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.133.0",
    "uvicorn[standard]>=0.41.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
]
